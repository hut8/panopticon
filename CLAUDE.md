# Claude Code Guidelines

## Git

- When merging PRs, use `gh pr merge --auto --merge`, not `--squash`. Squash merges are disabled on this repository.

## JavaScript / Frontend

- Always use `bun` instead of `npm`. Never use `npm`.
