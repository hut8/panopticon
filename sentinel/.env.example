# Sentinel firmware configuration — copy to .env and fill in real values.
# These are embedded at compile time via build.rs.

WIFI_SSID=your_wifi_ssid
WIFI_PASS=your_wifi_password
PANOPTICON_URL=https://your-panopticon-server.example.com
SENTINEL_SECRET=generate_a_random_32char_hex_string
